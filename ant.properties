jboss=C:/Users/elopa/Desktop/Labs (old)/Academ/Web-Programming/Lab2/wildfly-18.0.0.Final/wildfly-18.0.0.Final
jboss.deployment=${jboss}/standalone/deployments

build.dir=build
build.classes=${build.dir}/classes
build.classes.main.dir=${build.classes}/main
build.classes.test.dir=${build.classes}/test

docs.dir=docs

src.dir=src
src.main.dir=${src.dir}/main
src.test.dir=${src.dir}/test

web.dir=web
web-inf.dir=${web.dir}/WEB-INF
web.classes.dir=${web-inf.dir}/classes
web.lib.dir=${web-inf.dir}/lib

jar.file.name=MISPI-Lab3.jar
war.file.name=MISPI-Lab3.war
war.file.name.deployed=MISPI-Lab3.war.deployed

ivy.install.version=2.1.0-rc2
ivy.home=${user.home}/.ant
ivy.jar.dir=${ivy.home}/lib
ivy.jar.file=${ivy.jar.dir}/ivy.jar
ivy.report.dir=${build.dir}/ivy-reports

lib.dir=lib

params=
